import PhotosPage from '../features/photos-page';

const routesConfig = {
    photos: {
        path: '/',
        component: PhotosPage
    },
    users: {
        path: '/users'
    }
}

export default routesConfig;